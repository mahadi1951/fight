import React from 'react';

const AuthProvider = () => {
    return (
        <div>
            <h1>Auth Provider</h1>
            <p>This component will provide authentication context to the app.</p>
        </div>
    );
};

export default AuthProvider;